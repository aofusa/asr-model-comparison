{
  "name": "amcp-frontend",
  "version": "0.1.0",
  "description": "ASR Model Comparison Platform Frontend (Qwik) - Focused on Qwen3-ASR and Voxtral real-time",
  "license": "Apache-2.0 OR MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "qwik build",
    "build.client": "vite build",
    "postbuild": "node scripts/ensure-static-shell.js",
    "dev": "vite --mode ssr",
    "preview": "qwik preview",
    "test:e2e": "playwright test",
    "test:e2e:prod": "playwright test --config=playwright.config.prod.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "ignore": "^7.0.5",
    "vite": "^5.4.0",
    "vite-tsconfig-paths": "^4.2.0"
  },
  "dependencies": {
    "@builder.io/qwik": "^1.20.0",
    "@builder.io/qwik-city": "^1.20.0"
  }
}
