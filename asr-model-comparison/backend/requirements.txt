# ASR Model Comparison Platform (AMCP) - Backend Dependencies
# Python 3.12+

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.9.0

# ASR Models
# === Primary (start here) ===
faster-whisper>=1.0.3          # Whisper models - best CPU support, start with this

# === Future / heavier models ===
# Qwen3-ASR (official package may differ - check https://github.com/QwenLM/Qwen3-ASR)
# transformers>=4.46.0
# torch>=2.5.0
# torchaudio>=2.5.0

# Audio processing
pydub>=0.25.1

# Testing (required for TDD)
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0

# Resource monitoring (optional, for VRAM/CPU tracking)
psutil>=6.0.0

# Code quality (per CLAUDE.md)
black>=24.10.0
ruff>=0.7.0

# Platform-specific installation instructions:
# See INSTALL.md for CPU-only setup on ROG Ally X (AMD) and Apple Silicon.
# The basic TDD environment (no real ASR models) is already usable without these.
